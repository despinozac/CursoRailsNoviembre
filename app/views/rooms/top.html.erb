<h1>Top</h1>

<table border="1">
  <thead>
    <tr>
      <th>Nombre Sala</th>
      <th>No Reservaciones</th>
    </tr>
  </thead>

  <tbody>
    <% @rooms.each do |room| %>
      <tr>
        <td><%= room.name %></td>
        <td><%= room.cuenta %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br/>

<%= link_to "Volver", :back %>
